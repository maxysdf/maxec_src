apiVersion: v1
kind: Service
metadata:
  name: maxec-biz-customer
  labels:
    app: maxec-biz-customer
spec:
  type: NodePort
  ports:
  - port: 13500
    name: ap
    targetPort: 13500
    protocol: TCP
  selector:
    app: maxec-biz-customer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: maxec-biz-customer-deployment
  labels:
    app: maxec-biz-customer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: maxec-biz-customer
  template:
    metadata:
      labels:
        app: maxec-biz-customer
    spec:
      containers:
      - name: maxec-biz-customer
        image: host.docker.internal:5000/maxec-biz-customer:BRANCH-VERSION # will be replaced by jenkins
        env:
        - name: DBURL     
          value: jdbc:postgresql://maxec-db/maxec
        - name: DBDRV     
          value: org.postgresql.Driver
        - name: DBUSER
          value: maxec
        - name: DBPASS
          value: maxec
        - name: DBDIALECT 
          value: org.hibernate.dialect.PostgreSQL9Dialect
        ports:
        - containerPort: 13500
  